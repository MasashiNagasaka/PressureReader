---
name: run-{{PROJECT_NAME}}
description: {{PROJECT_NAME}} の起動・テスト・確認手順。/run や /verify から自動参照される。
allowed-tools: Bash
---

# {{PROJECT_NAME}} 起動・確認レシピ

> `/run-skill-generator` により自動生成。変更があれば再実行するか手動で編集してください。

## インストール

```bash
{{INSTALL_COMMAND}}
```

## 起動コマンド

```bash
{{START_COMMAND}}
```

## テスト・型チェック

```bash
{{TEST_COMMAND}}
```

## ビルド

```bash
{{BUILD_COMMAND}}
```

## 確認 URL / ポート

```
{{APP_URL}}
```

## 必要な環境変数（名前のみ）

> 値は `.env` ファイルで管理すること。このファイルには記録しない。

{{ENV_VARS_LIST}}

## よく失敗するポイント・注意事項

{{NOTES}}

---

## /run・/verify 向けガイド

このレシピを参照して起動・確認を行う場合:

1. **インストール確認**: 依存関係がインストールされているか確認する
2. **環境変数確認**: 上記の環境変数が `.env` または環境に設定されているか確認する
3. **起動**: 上記の起動コマンドを実行する
4. **確認**: 確認 URL にアクセスし、主要機能の動作を確認する
5. **テスト**: テストコマンドを実行し、全テストが通ることを確認する
