# Agent Instructions

このリポジトリでは、開発方針・設計思想・作業ルールを `CLAUDE.md` に集約している。

作業開始時は、必ずリポジトリルートの `CLAUDE.md` を読み、その内容をこのリポジトリの主要な開発指示として扱うこと。

`AGENTS.md` と `CLAUDE.md` の内容が矛盾する場合は、`CLAUDE.md` を優先すること。

`.claude/commands` と `.claude/skills` が存在する場合は、それらもこのプロジェクトの開発ワークフロー定義として参照すること。