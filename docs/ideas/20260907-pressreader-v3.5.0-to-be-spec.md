# PressReader v3.5.0 改修仕様（To-Be）

## 1. 改修対象
- 現行: `C:\nagasaka\AI\PressureReader\py\PressReader_v3.4.2.py`
- 改修後: `C:\nagasaka\AI\PressureReader\py\PressReader_v3.5.0.py`
- バージョン表示:
  - 改修前: `3.4.2`
  - 改修後: `3.5.0`

## 2. 本改修の必須要件
1. 実行対象ファイル名を `PressReader_v3.5.0.py` に統一する。
2. アプリ内部の版数文字列を `3.5.0` に変更する。
3. スプラッシュ画面とメイン画面タイトルの表示バージョンを `v3.5.0` にする。
4. 既存機能（画像読込、範囲選択、圧力計算、メタ保存、Excel出力、PDF=>PNG変換）の挙動を維持する。

## 3. スコープ
### 3.1 今回含めるもの
- ファイル名の改名:
  - `PressReader_v3.4.2.py` -> `PressReader_v3.5.0.py`
- バージョン定数・表示文言の更新:
  - `version = "3.4.2"` -> `version = "3.5.0"`
  - 画面表示上の`v3.4.2`相当箇所を`v3.5.0`へ更新
- ドキュメント上の対象ファイル名更新:
  - As-Is/To-Be文書の参照整合

### 3.2 今回含めないもの
- 換算ロジック（`c2p`）の式変更
- しきい値・測定レンジの仕様変更
- UIレイアウト刷新
- 新機能追加
- データフォーマット変更（PNGメタデータ`press{...}`、Excelレイアウト等）

## 4. 互換性要件
1. 既存のPNGメタデータ形式（`press{...}`）を読み書きできること。
2. 既存の解析オペレーション手順を変更しないこと。
3. 旧版で扱っていた入力データ（PNG/PDF）を継続利用できること。

## 5. 実装方針
1. まずファイル名改名を実施し、参照箇所の不整合を解消する。
2. バージョン定数と表示ラベルを更新する。
3. 起動確認と主要導線のスモークテストで回帰確認を行う。
4. 影響箇所を最小化し、ロジック変更は行わない。

## 6. 受け入れ条件（Acceptance Criteria）
1. `PressReader_v3.5.0.py` が存在し、起動可能である。
2. スプラッシュ画面に `v3.5.0` が表示される。
3. メイン画面タイトルに `PressureReader v3.5.0` が表示される。
4. 次の主要機能が従来通り動作する:
   - PNG読込
   - 解析条件の保存/復元
   - 四角形/多角形/円形の範囲選択
   - 検出値表示
   - Excel出力
   - PDF=>PNG変換
5. 既存の`press{...}`メタデータ付きPNGで復元処理が成功する。

## 7. テスト観点
1. 起動・表示:
   - スプラッシュ版数
   - タイトル版数
2. 入出力:
   - PNGメタ保存後に再読込して値が一致する
   - xlsx出力が正常完了する
3. 操作:
   - 3つの範囲選択モードで計算実行できる
   - 測定範囲可視化ON/OFFが動作する
4. 変換:
   - PDF=>PNGで出力ファイルが生成される

## 8. リスクと対策
- リスク: 改名により起動手順や外部参照が壊れる
  - 対策: 参照パスを棚卸しし、実行確認を必ず実施する
- リスク: バージョン表記の更新漏れ
  - 対策: `3.4.2`文字列の検索で残存確認を行う
- リスク: 見た目は変わらないため不具合が埋もれる
  - 対策: 主要導線のスモークテストをチェックリスト化して確認する

## 9. 完了条件
1. `PressReader_v3.5.0.py` への改名が完了している。
2. 版数表示が `3.5.0` に統一されている。
3. 受け入れ条件を満たすテスト結果が確認されている。
4. 必要に応じて関連ドキュメントが更新されている。
