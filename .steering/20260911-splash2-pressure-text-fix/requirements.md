# Requirements: スプラッシュ2 文言修正（圧力計測時）

## 背景
- スプラッシュ2の2行目右側説明が `圧力計測時の設定値` になっている。
- 最新仕様では `圧力計測時の値` に統一し、強調表示（マゼンダ+bold）にする。

## 要件
- `show_startup_info_window()` の2行目右側テキストを `圧力計測時の値` に変更する。
- 上記テキストをキーワード強調ルールに合わせて表示する。

## 非対象
- スプラッシュ表示順・表示時間・他行の文言変更は行わない。
