# Requirements: 範囲確定前の点圧表示（クリック1点）

## 目的
- 範囲確定前でも、クリックした点の圧力値(MPa)を画像上で確認できるようにする。

## 要件
- 点クリック時に、クリック点近傍へ `点圧 xx.xxxMPa` を表示する。
- 四角形/円形はドラッグ確定前、 多角形は右クリック確定前まで点圧表示を優先する。
- 範囲確定後は従来どおり範囲統計表示（平均/最大/最小 + 面積表示）へ移行する。
- 既存の解析条件ガードは維持する。
