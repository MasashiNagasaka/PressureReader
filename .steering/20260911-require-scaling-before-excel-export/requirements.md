# Requirements: Excel出力前スケーリング必須化（押下時メッセージ）

## 目的
- スケーリング未設定でのExcel出力を防ぎ、ユーザーに設定を促す。

## 要件
- `選択範囲Excel出力` ボタン押下時に `pixmm_entry` を検証する。
- 未設定/数値変換不可/0以下の場合は、メッセージ表示して出力を中断する。
- スケーリングが有効な正数なら、従来どおり出力処理を実行する。
- ボタンの有効/無効状態は変更しない。
