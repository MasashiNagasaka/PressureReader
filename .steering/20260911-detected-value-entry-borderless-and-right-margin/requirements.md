# Requirements: 検出値表示欄の枠線除去と右端重なり解消

## 目的
- 検出値表示欄の不要な枠線をなくす。
- スクロールバー表示有無に関係なく、右端と重ならない見え方にする。

## 要件
- `検出値（平均）` / `検出値（最大）` / `検出値（最小）` の値表示欄を枠線なし表示にする。
- 値表示欄フォーカス時にも不要な強調枠を表示しない。
- 検出値エリア右側に余白を確保し、`MPa` 表示が右端に詰まらないようにする。
- 検出値の計算/表示ロジックは変更しない。
