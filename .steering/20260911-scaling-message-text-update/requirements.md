# Requirements: スケーリング未設定メッセージ文言更新

## 目的
- Excel出力時のスケーリング未設定メッセージを、操作誘導が明確な文言に更新する。

## 要件
- `save_brightness_to_xlsx()` のスケーリング未設定ガードで表示する文言を以下へ変更する。
  - `スケーリングボタンでスケール(画像の寸法)を設定してください`
- 既存のガード条件（空欄/数値変換不可/0以下）と表示方式（中央オーバーレイ2秒）は維持する。
