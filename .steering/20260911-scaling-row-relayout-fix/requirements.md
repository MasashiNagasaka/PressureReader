# Requirements: スケーリング行レイアウト再設計

## 目的
- `pixmm_entry` の表示欠けと、`mm/px` の直右非表示（大きな空白）を同時に解消する。

## 要件
- スケーリング行を独立フレーム化し、内部で横並びを固定する。
- `pixmm_entry` は `0.017614` を欠けずに表示できる幅を確保する。
- `mm/px` は `pixmm_entry` のすぐ右に表示する。
- スクロールバー表示有無の両状態で見え方が崩れない。
- スケーリング設定ロジックは変更しない。
