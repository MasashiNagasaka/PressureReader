# Requirements: 円形ハンドルの独立移動（反対側固定）

## 目的
- 円形モードのハンドル操作で、ドラッグした片側のみを動かせるようにする。

## 要件
- 左右ハンドルは、掴んだ側のみ移動し反対側は固定する。
- 上下ハンドルは、掴んだ側のみ移動し反対側は固定する。
- 幅/高さが 0 以下にならないよう最小サイズを維持する。
- 円内部ドラッグ移動、確定後の再計算、点圧表示優先ルールは既存維持とする。
