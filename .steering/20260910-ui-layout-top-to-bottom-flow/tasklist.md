# Tasklist: ui-layout-top-to-bottom-flow

- [x] 既存UI配置（`grid(row=...)`）の実装位置を確認
- [x] 明度欄の動的配置開始行を上位フローに合わせて変更
- [x] 画像準備ボタン（PDF→PNG、PNGを開く）を上部へ移動
- [x] `PDF→PNG変換` の直上に `画像準備：` ラベル（既存フォーマット）を追加
- [x] 解析条件設定ブロックの順序を仕様どおりに再配置
- [x] `PNGに解析条件を保存` を `解析条件をコピー` の直上へ移動
- [x] 保存/コピーボタンの上下逆転不整合を修正（仕様どおり: 保存が上、コピーが下）
- [x] 範囲選択ブロックを表示調整ブロックより上に移動
- [x] 出力・画面リセットを下部へ再配置
- [x] `python -m py_compile src/PressureReader.py` で構文確認
