# Design

## Approach
- `label_slider` の `text` を `選択範囲の高圧検出箇所：` に変更する。
- `表示数の設定` はスライダー上段（直前行）に配置する。
- スライダー行は左に `slider_value` の現在値表示を置き、右にスライダー本体を配置する。
- スライダーの長さは元のサイズへ戻し、右横追加のための短縮は行わない。

## Impact
- 変更対象は `src/PressureReader.py` のラベル定義周辺。
- 下部セクション（出力/入力内容クリア）の行番号を1段後ろへずらして重なりを防ぐ。
