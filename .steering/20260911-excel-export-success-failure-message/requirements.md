# Requirements: Excel出力の成功/失敗メッセージ表示

## 目的
- `選択範囲Excel出力` 実行後に、保存成功/失敗をユーザーへ明示する。

## 要件
- 保存成功時に `Excelファイルを出力しました` を表示する。
- 保存失敗時に `Excelファイルの出力に失敗しました` を表示する。
- 保存ダイアログをキャンセルした場合は、メッセージを表示しない。
- 既存のオーバーレイ表示パターン（中央ラベル + 自動消去）に合わせる。
