# Requirements: スケーリング単位ラベル（mm/px）表示欠け修正

## 目的
- スケーリング行の単位ラベル `mm/px` が欠けずに常時表示されるようにする。

## 要件
- スクロールバー表示/非表示の両状態で `mm/px` が全文字表示されること。
- 単位ラベル側に表示幅を固定確保し、`mm/` や `mm` までの欠けを防ぐこと。
- `pixmm_entry` の直右に `mm/px` を表示し、不自然な大きい空白を作らないこと。
- `ｽｹｰﾘﾝｸﾞ` ボタン位置は変更しない。
- `pixmm_entry` の設定・表示動作は維持する。
