# Requirements: 選択範囲Excel出力の多角形/円形対応

## 目的
- `選択範囲Excel出力` を四角形だけでなく、多角形・円形モードでも利用可能にする。

## 要件
- 四角形/多角形/円形の各モードで、選択範囲をExcelへ出力できる。
- 未選択状態では既存メッセージ `選択範囲を作成してください` を表示する。
- 多角形/円形では選択外を空欄(`None`)で出力する。
- 既存の圧力換算ルール（通常値、999、-999、空欄）を維持する。
