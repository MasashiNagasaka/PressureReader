# Requirements: 解析条件コピー CANCEL時のメイン復帰

## 目的
- `解析条件をコピー` 処理でダイアログを `CANCEL` した際、アプリ終了せずメイン画面へ戻す。

## 要件
- コピー元選択 `CANCEL` 時は処理中断し、アプリは継続する。
- コピー先選択 `CANCEL` 時は処理中断し、アプリは継続する。
- メタデータ読取失敗時は警告表示して処理中断し、アプリは継続する。
- 正常系のメタコピー処理は維持する。
