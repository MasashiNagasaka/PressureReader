# Requirements

## Goal
スケーリング設定値の単位を画面上で明示し、値の意味を誤認しにくくする。

## Functional Requirements
- スケーリング行を `スケーリング` ボタン + 設定値欄 + `mm/px` 表示にする。
- `スケーリング` ボタンの位置は変更しない。
- 設定値欄の横幅を現行より狭める。
- `mm/px` ラベルは常時表示する。

## Non-Goals
- 換算計算ロジックの変更。
- ダイアログ挙動の変更。
