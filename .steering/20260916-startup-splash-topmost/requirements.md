# Requirements: 起動時スプラッシュとメイン初期表示の最前面化

## 目的
- 起動時に表示されるスプラッシュ1/2、およびメイン画面初期表示を前面表示し、起動導線を見失いにくくする。

## 要件
1. スプラッシュ1（ロゴ）を最前面で表示する。
2. スプラッシュ2（事前準備）を最前面で表示する。
3. メイン画面は初期表示時に最前面へ上げる。
4. メイン画面は常時最前面固定にせず、初期表示後は通常状態へ戻す。
5. 既存表示時間（スプラッシュ1: 約2秒、スプラッシュ2: 約8秒）を維持する。
