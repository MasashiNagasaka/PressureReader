# Requirements: Excel出力セルの緑・黄・赤カラースケール

## 背景
- Excel出力後に圧力分布を直感的に把握できるよう、出力セル全体を色分けしたい。

## 要件
- `save_brightness_to_xlsx` の出力セル範囲全体に、条件付き書式の3色カラースケールを適用する。
- 色は「低値=緑 / 中間=黄 / 高値=赤」とする。
- 既存の出力値、セル寸法、ズーム調整、保存処理は変更しない。

## 制約
- 追加パッケージは導入しない（既存 `openpyxl` のみ使用）。
- 仕様書・コードの日本語文字化けを発生させない。
