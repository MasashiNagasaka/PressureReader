# Requirements: 検出値表示欄の縦間隔調整（温度/湿度欄準拠）

## 目的
- 検出値3項目の値表示欄が縦方向にくっついて見える状態を解消する。

## 要件
- `検出値（平均）` / `検出値（最大）` / `検出値（最小）` の値表示欄が視覚的に分離して見えること。
- 見え方は `温度[℃]` / `湿度[%]` 欄の分離感を参考にする。
- ラベル文言・フォントサイズ・横方向レイアウトは変更しない。
- 検出値の計算/表示ロジックは変更しない。
