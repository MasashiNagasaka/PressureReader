# Requirements: 解析条件不足メッセージの具体化

## 目的
- 解析条件不足時に、ユーザーが次に何を設定すべきか分かる具体メッセージを表示する。

## 要件
- 明度未設定時は最優先で案内する。
- 温度/湿度は不足状態に応じて文言を出し分ける。
- 既存の中央オーバーレイ表示（2秒）を維持する。
- 解析ロジックや既存メッセージ系（未画像、未範囲、スケーリング未設定）に影響を与えない。
