# Requirements: 標準色見本処理ボタン トグル化

## 目的
- 標準色見本処理の選択状態（ON/OFF）を明示し、ユーザーが現在モードを判断しやすくする。

## 要件
1. `標準色見本処理` ボタンをトグル挙動にする。
2. OFFで押下すると色見本モード（`set_mode_rect(\"14\")`）へ遷移する。
3. ONで押下すると通常矩形モード（`set_mode_rect(\"99\")`）へ戻る。
4. 色見本矩形を確定した後は自動でOFFへ戻る。
5. 既存の明度読取・入力反映・通常解析動作に回帰を起こさない。
